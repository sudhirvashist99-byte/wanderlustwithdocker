FROM node:18-alpine

WORKDIR /app

# Copy dependency files
COPY package*.json ./

# Install all dependencies (with nodemon, tsx, etc.)
RUN npm install

# Copy the rest of the code
COPY . .

# Expose backend dev port
EXPOSE 8080

# Run in watch mode
CMD ["npm", "run", "dev"]
